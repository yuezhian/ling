除了.git文件夹就都是是工作区的文件夹
.git文件夹是本地版本库，本地版本库中有一个缓存区，当我们把工作区的还不受本地版本库管理的文件"添加"上来时只是把不受本地版本库控制的文件添加到缓存区，只要再次"提交"才是把文件从缓存区提交到本地版本库，如果要把本地版本库"推送"到远端服务器的版本库，就要把本地的版本库链接远端库然后把本地版本库"推送"到远端服务器版本库。

把文件"提交"到本地版本库不需要网络的，只有把本地版本库"推送"到远端版本库时才需要网络。